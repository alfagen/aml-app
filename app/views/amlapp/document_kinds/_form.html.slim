h3 Тип документа для группы "#{document_group.title}"
= simple_form_for document_kind do |f|
  = f.input :aml_document_group_id, as: :hidden
  = f.input :title, label: false, hint: 'Название типа документа'
  = f.input :details, label: false, hint: 'Описание документа'
  = f.input :position, label: false, hint: 'Номер документа'
  = f.file_field :image, hint: 'Изображение документа'
  = f.input :file_title, label: false, hint: 'Название изображения'
  = f.button :submit, 'Создать/изменить', class: 'btn btn-success'
